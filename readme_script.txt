-----------------------------------------------Содержание-----------------------------------------------

wellnet/
	calculation/
		__init__.py
		calculation_wells.py
		check_first_row_wells.py
		optim_mesh.py
		regular_mesh.py
		shapely_geometry.py
		support_functions.py

	gui/
		__init__.py
		calculation_thread.py
		log_window.py
		main_window.py
		mpl_widget.py
		plot_design.py
		qtsample.py
		validate_widget_data.py

	input_output/
		__init__.py
		dictionaries.py
		preparing_data.py
		save_database.py
	
	__init__.py
	main.py


-----------------------------------------------Инструкция-----------------------------------------------

--- main.py

	Основной скрипт модуля. Вызывает запуск приложения PyQt.

--- calculation_wells.py

	Содержит основную функцию расчета для построения опорной сетки по выбранному сценарию.

	-> calculation(polygon, df_in_contour, contour_name, path_property, list_exception,
                       dict_parameters, log_user, progress_bar)

		Основная функция расчета

    		- polygon: контур, заданный пользователем
    		- df_in_contour: DataFrame из скважин, находящихся внутри контура
    		- contour_name: имя контура
    		- path_property: путь к справочнику с PVT свойствами
    		- list_exception: список исключаемых скважин
    		- dict_parameters: словарь с параметрами расчета
    		- log_user: сигнал, передающий сообщения для пользователя в окно логирования приложения
    		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

    		Возвращает словарь с результирующим DataFrame по каждому ключу

--- check_first_row_wells.py

	Скрипт для определения среднего и минимального радиуса первого ряда окружения каждой скважины объекта.

	-> check_well_intersection(df_intersectionWells, MaxOverlapPercent):

    		Проверка пересечений между скважинами в исходном массиве, для исключения перекрываемых скважин второго ряда

   		- df_intersectionWells: массив скважин с исходными данными
    		- MaxOverlapPercent: максимальный процент от общей длины ствола скважины, который может быть скрыт при
                             	     пересечении, чтобы скважина осталась в первом ряду

    		Возвращает список скважин за первым рядом, которые требуется удалить

	-> first_row_of_well_geometry(df_WellOneArea, wellNumberInj,
                               verticalWellAngle, MaxOverlapPercent,
                               angle_horizontalT1, angle_horizontalT3):

    		Поиск для нагнетательной скважины окружения первого ряда на основе геометрии

   		- angle_horizontalT3: sector expansion angle for horizontal well's T1 point
    		- angle_horizontalT1: sector expansion angle for horizontal well's T3 point
   		- df_WellOneArea: массив скважин окружения с расстоянием до нагнетательной < maximumDistance
    		- wellNumberInj: номер рассматриваемой нагнетательной скважины
    		- verticalWellAngle: угол для обозначения зоны вертикальных скважин на карте
   		- MaxOverlapPercent: максимальный процент от общей длины ствола скважины, который может быть скрыт при
                                   пересечении, чтобы скважина осталась в первом ряду

    		Возвращает список скважин первого ряда

	-> mean_radius(df_in_contour, verticalWellAngle, MaxOverlapPercent,
                angle_horizontalT1, angle_horizontalT3, max_distance):

    		Расчет среднего и минимального радиусов первого ряда окружения по объекту

 		- df_in_contour: DataFrame скважин, попавших в контур
  		- verticalWellAngle: угол расширения сектора вертикальных скважин в полярной системе координат
   		- MaxOverlapPercent: максимальный процент перекрытия от общей длины скважины
   		- angle_horizontalT1: угол расширения сектора T1 ГС в полярной системе координат
    		- angle_horizontalT3: угол расширения сектора T3 ГС в полярной системе координат
    		- max_distance: максимальный радиус для выделения скважин первого окружения

   		Возвращает средний радиус по объекту и DataFrame с добавленными столбцами минимального расстояния
		первого окружения для каждой скважины и средним радиусом для всех скважин текущего объекта расчета

--- optim_mesh.py

	Скрипт для построения сетки исследований с охватом всего добывающего фонда (оптимальный сценарий).

	-> calc_optim_mesh(df_prod_wells, df_piez_wells, df_inj_wells, df_proj_wells,
                    df_result, df_necessarily_wells, horizon, mean_rad, coeff, key,
                    obj_square, path_property, list_exception, dict_parameters, log_user)

		Функция расчета оптимальной опорной сетки, включающая в себя все функции обработки отдельных типов скважин
		
		- df_prod_wells: DataFrame добывающих скважин
		- df_piez_wells: DataFrame пьезометрических скважин
		- df_inj_wells: DataFrame нагнетательных скважин
		- df_proj_wells: DataFrame проектных скважин
		- df_result: результирующий DataFrame
		- df_necessarily_wells: DataFrame обязательных для исследования скважин
		- horizon: текущий объект расчета
		- mean_rad: средний радиус первого ряда окружения по объекту
		- coeff: коэффиуиент кратного увеличения радиуса
		- key: результирующего ключ словаря
		- obj_square: площадь объекта по крайним скважинам с отступом на средний радиус исследования
		- path_property: путь к файлу с параметрами
		- list_exception: список исключаемых из расчета скважин, "слепых" зон и скважин в них
		- dict_parameters: словарь с параметрами (коэффициенты на радиус, углы перекрытия и тд)
		- log_user: сигнал, передающий сообщения для пользователя в окно логирования приложения
 		
    		Возвращается словарь с добавленным ключом по коэффициенту умножения радиуса охвата

	-> core_optim_mesh(list_prod_exception, path_property, percent, mean_rad, coeff, horizon,
                    obj_square, min_time_research, max_time_research, calc_option, limit_research_time,
                    df_piez_wells, df_prod_wells, df_inj_wells, df_result, df_necessarily_wells):

		Функция для расчета результирующего DataFrame по объекту

		- list_prod_exception: список исключаемых из расчета скважин
    		- path_property: путь к файлу со свойствами
    		- percent: процент длины траектории скважины для включения в зону охвата
    		- mean_rad: средний радиус по объекту
    		- coeff: коэффициент кратного увеличения радиуса
    		- horizon: объект, по которому идет расчет
    		- obj_square: площадь объекта месторождения по краевым скважинам
    		- min_time_research: ограничение минимального времени исследования ННС
    		- max_time_research: ограничение максимального времени исследования ННС
    		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата
    		- limit_research_time: параметр учета границ времени исследования
    		- df_piez_wells: пьезометры по текущему объекту
    		- df_prod_wells: добывающие скважины по текущему объекту
    		- df_inj_wells: нагнетательные скважины по текущему объекту
    		- df_result: пустой DataFrame, в который записывается результат расчета
    		- df_necessarily_wells: DataFrame с обязательными скважинами

    		Возвращает результирующий DataFrame по объекту

	-> piez_calc(df_piez_wells, hor_prod_wells, df_result, percent, calc_option)

		Функция обрабатывает DataFrame из пьезометров, подающийся на вход

		- df_piez_wells: DataFrame из пьезометров, выделенный из входного файла
		- hor_prod_wells: DataFrame из добывающих скважин
		- df_result: В функцию подается DataFrame df_result для добавления в общий результат расчета пьезометров
		- percent: процент длины траектории скважины для включения в зону охвата
		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата

		Возвращаются: 1) список скважин, не имеющих пересечений;
                              2) DataFrame пьезометров;
                              3) DataFrame добывающих;
                              4) Общий DataFrame со всеми результатами расчета по объекту

	-> inj_calc(isolated_wells, hor_prod_wells, df_inj_wells, df_result, percent, calc_option)

		Функция обарабатывает DataFrame нагнетательных скважин

		- isolated_wells: Список скважин, не имеюших пересечений
		- hor_prod_wells: DataFrame добывающих скважин
		- df_inj_wells: DataFrame нагнетательных скважин
		- df_result: Результирующий DataFrame, к которому добавится результат обработки DataFrame нагнетательных скв.
		- percent: процент длины траектории скважины для включения в зону охвата
		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата

		Возвращаются: 1) список скважин, не имеющих пересечений;
                              2) DataFrame нагнетательных;
                              3) DataFrame добывающих;
                              4) Общий DataFrame со всеми результатами расчета по объекту

	-> single_calc(list_exception, isolated_wells, hor_prod_wells, df_result, percent, calc_option)

		Функция обарабатывает DataFrame одиночных скважин

		- list_exception: список исключаемых из расчета скважин
		- isolated_wells: Список скважин, не имеюших пересечений
		- hor_prod_wells: DataFrame добывающих скважин
		- df_result: Результирующий DataFrame, к которому добавится результат обработки DataFrame одиночных скв.
		- percent: процент длины траектории скважины для включения в зону охвата
		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата

		Возвращаются: 1) список скважин, не имеющих пересечений;
                              2) DataFrame добывающих;
                              3) Общий DataFrame со всеми результатами расчета по объекту

	-> get_invisible_wells(df_recalc, df_prod, percent, radius, coeff, calc_option)

		Функция получения скважин в слепой зоне при k > ограничения коэффициента кратного увеличения R (k*R)

		- df_recalc: копия результирующего DataFrame для выделения скважин в слепой зоне
		- df_prod: DataFrame добывающих скважин
		- percent: процент перекрытия зоной охвата, при котором скважина попадает в нее
		- radius: максимальный радиус охвата в слепой зоне
		- coeff: коэффициент домножения радиуса
		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата

		Возвращает список скважин для дообследования и DataFrame с обновленным столбцом пересечений

	-> separation_gdis(df_invisible)

		Функция разделения скважин в слепых зонах на 2 года

		- df_invisible: DataFrame скважин, которые попали в слепую зону

		Возвращает два DataFrame, по которым распределены скважины в слепой зоне(каждая вторая)

	-> optimization(df_prod, df_inj_piez)

		Выделяется список нагнетательных/пьезометров из DataFrame продуктивных, имеющих 1 пересечение.
		Оптимизация заключается в переопределении списка нагн/пьез. с помощью исключения скважин, входящих
		как в список пересечений, так и в список исключений, из df_optim

		- df_prod: DataFrame добывающих скважин
		- df_inj_piez: DataFrame нагнетательных/пьезометров

		Возвращает обновленный список нагнетательных/пьезометров

--- regular_mesh.py

	Скрипт, содержащий функцию построения регулярной сетки всех фондов с учетом их порядка, указанного пользователем.
	Регулярная сетка строится на основе кол-ва охваченнных радиусом исследования скважин.

	-> calc_regular_mesh(df_prod_wells, df_piez_wells, df_inj_wells, df_proj_wells, df_result, df_necessarily_wells,
			     horizon, path_property, dict_parameters, obj_square, mean_rad, coeff, list_exception, log_user)

    		Расчет регулярной сетки скважин

		- list_exception: список скважин для исключения из ОС
		- df_necessarily_wells: DataFrame скважин, обязательных для включения в ОС
		- df_proj_wells: DataFrame проектных скважин
		- df_prod_wells: DataFrame добывающих скважин на текущий объект расчета
		- df_piez_wells: DataFrame пьезометрических скважин на текущий объект расчета
		- df_inj_wells: DataFrame нагнетательных скважин на текущий объект расчета
		- df_result: результирующий DataFrame содержащий опорные скважины
		- horizon: текущий объект расчета
		- path_property: путь к файлу с PVT свойствами
		- dict_parameters: словарь с параметрами расчета
		- obj_square: площадь текущего объекта расчета по крайним скважинам
		- mean_rad: средний радиус исследования по текущему объекта
		- coeff: коэффициент кратного увеличения радиуса исследования
		- log_user: сигнал, передающий сообщения для пользователя в окно логирования приложения

		Возвращает результирующий DataFrame с опорными скважинами

--- shapely_geometry.py

	Все функции, связанные с пересечением/охватом зонами исследования скважин. Также скрипт содержит функцию оптимизации
	пересечений первого сценария расчета с приоритетом охвата всего добывающего фонда и функцию загрузки контура
	пользователя по координатам из .txt файла
	
	-> get_polygon_well(R_well, type_well, *coordinates)

		Создание зоны вокруг скважины с заданным радиусом

		- R_well: радиус создания зоны
		- type_well: тип скважины
		- coordinates: координаты устье/забой

		Возвращает геометрический объект зоны вокруг скважины

	-> check_intersection_area(area, df_points, percent, calc_option)

		Проверка входят ли скважины из df_point в зону другой скважины area

		- percent: процент попадания скважины в зону охвата area
		- calc_option: флаг переключения сценария охвата скважин
		- area: координаты зоны вокруг конкретной скважины
		- df_points: данные из которых берется геометрия скважин(точки/линии)

		Возвращаются имена скважин, которые входят в данную зону area

	-> check_intersection_point(point, df_areas, percent, calc_option)

		Функция позволяет узнать, перечесение со сколькими зонами имеет определенная скважина

		- calc_option: флаг переключения сценария охвата скважин
		- percent: процент попадания скважины в зону охвата
		- point: геометрия скважины(точка/линия)
		- df_areas: DataFrame со столбцом зон вокруг скважин

		Возвращает пересечение со сколькими зонами имеет определенная скважина

	-> intersect_number(df_prod, df_inj_piez, percent, calc_option)

		Функция добавляет в DataFrame столбец 'intersection', в него записываются
    		имена скважин из другого DataFrame, с которыми пересекается текущая, затем добавляется столбец 'number',
    		в который заносится кол-во пересечений конкретной скважины с остальными

		- calc_option: параметр определяет критерий учета процента длины ГС для попадания в зону охвата
		- percent: процент попадания скважины в зону охвата
		- df_prod: добывающие
		- df_inj_piez: нагнетательные/пьезометры

		Возвращаются DataFrame с кол-вом пересечений

	-> add_shapely_types(df_input, mean_rad, coeff)

		Добавление в DataFrame столбца с площадью охвата скважин, в зависимости от среднего радиуса охвата по контуру

		- coeff: коэффициент домножения радиуса
		- df_input: DataFrame, полученный из исходного файла
		- mean_rad: средний радиус окружения для итерируемого объекта

		Возвращается DataFrame с добавленными столбцами геометрии и площади влияния каждой скважины

	-> get_contours(contours_path, log_user, progress_bar)

		Получение многоугольников контуров, заданных пользователем

		- contours_path: абсолютный путь к .txt файлу с координатами контуров
		- progress_bar: сигнал для изменения значения progress bar
		- log_user: сигнал для вывода логов в окно для пользователя

		Возвращает словарь с многоугольниками, построенными из координат контруров, ключами словаря будут названия файлов

--- support_functions.py

	Содержит вспомогательные функции для расчета

	-> unpack_status(dict_constant)

		Распаковка параметров из словаря, для удобства использования в коде

		- dict_constant: словарь со статусами скважин и характером их работы

		Возвращает статусы и характер работы скважин как отдельные переменные типа string

	-> get_property(path)

		Считывание параметров из yaml-файла в словарь

		- path: путь к файлу с параметрами

		Возвращает словарь с параметрами по ключу объекта

	-> wc_func(x, water_cut, const, S_o_init, S_w_init, Corey_w, Corey_o)

		Функция зависимости обводненности от водонасыщенности

		- x: начальное приближение для поиска корня уравнения
		- water_cut: обводненность
		- const: коэффициент перед функцией (начальная водонасыщенность, остаточная нефтенасыщенность, вязкости, проницаемости)
		- S_o_init: остаточная нефтенасыщенность
		- S_w_init: начальная водонасыщенность
		- Corey_w: степень Кори для воды
		- Corey_o: степень Кори для нефти

		Возвращает значение функции обводненности

	-> get_time_coef(dict_property, objects, Wc, oilfield, gas_status)

		Рассчет коэффициента для формулы по вычислению времени исследования скважины
    		При умножении этого коэффицента на радиус охвата, получаем время исследования

		- gas_status: тип скважины для выбора формулы расчета времени КВД (нефтяная, газовая, газоконденсатная,
			      водонагнетательная, газонагнетательная, поглощающая, пьезометрическая)
		- oilfield: название месторождения
		- Wc: обводненность
		- objects: название пласта
		- dict_property: словарь со свойствами всех пластов

	Возвращает коэффициент для расчета времени исследования
	
	-> dict_keys(list_r, contour_name)

		Создание словаря для записи сценариев расчета. Ключи словаря - связка (название контура + коэффициент увеличения R)

		- list_r: список коэффициентов для умножения радиуса
		- contour_name: имя контура

		Возвращает словарь с ключами из коэффициентов и имени текущего контура

	-> upload_parameters(path)

		Функция загрузки заданных пользователем параметров

		- path: путь к файлу с параметрами расчета

		Возвращает словарь с параметрами расчета

	-> get_path()

		Функция возвращает путь, по которому находится exe файл

	-> clean_work_horizon(df, count_of_hor)

		Удаляет из DataFrame скважины с числом объектов работы больше заданного пользователем

		- df: база данных
		- count_of_hor: максимальное кол-во объектов работы скважины, задается пользователем

		Возвращает DataFrame со скважинами, число объектов работы которых не превышает заданного пользователем кол-ва

	-> delete_logfiles(mypath)

		Удаление логфайлов предыдущих расчетов по указанному пути

		- mypath: абсолютный путь к папке с логфайлами

	-> rgb_to_ycc(r, g, b)

		Перевод цвета из RGB в YCbCr

		- r: число, характеризующее расстояние по вектору красного цвета в RGB
		- g: число, характеризующее расстояние по вектору зеленого цвета в RGB
		- b: число, характеризующее расстояние по вектору синего цвета в RGB

		Возвращает числа, переведенные из пространства RGB в пространство YCbCr

	-> to_ycc(color)

		Нормирование каждого числа RGB вектора

		- color: кортеж из оттенков цвета RGB

		Возвращает нормированный по оттенкам цвет RGB и переведенный в пространство YCbCr

	-> color_dist(c1, c2)

		Определяет расстояние цвета пользователя в RGB до одного из основных цветов пространства

		- c1: цвет в RGB
		- c2: один из основных цветов в RGB (итерация по основным цветам)

		Возвращает квадрат евклидова расстояния между двумя векторами цветов в пространстве YUV

	-> min_color_diff(color_to_match, colors)

		Возвращает минимальное расстояние до одного из цветов в пространстве YCbCr и имя цвета

		- color_to_match: цвет, оттенок которого нудно определить
		- colors: цвет из списка основных цветов RGB

		Возвращает минимальное расстояние до одного из основных цветов RGB и название цвета

--- calculation_thread.py

	Содержит класс запуска расчета в параллельном потоке CalculationThread(QtCore.QThread)

	-> initialize_logging(self)

		Инициализация логов в файл

	-> run(self)

		Основной метод, выполняющий расчет в потоке

	-> load_and_prepare_data(self)

		Загружает и подготавливает данные для расчета

		Возвращает подготовленный DataFrame данных по скважинам, DataFrame исключенных скважин,
                список исключаемых скважин, путь к .json файлу с PVT свойствами по всем месторождениям

	-> process_contours(self, df_input, path_property, list_exception)

		Обрабатывает контуры и выполняет расчеты

		- df_input: DataFrame входных данных
		- path_property: путь к .json файлу с PVT свойствами по всем месторождениям
		- list_exception: список исключаемых из расчета скважин

		Возвращает словарь с результатами расчета по контурам и список скважин вне контуров

	-> process_wells_out_of_contours(self, df_input, well_out_contour, path_property, list_exception)

		Выполняет расчет для скважин вне контуров

		Возвращает словарь с результатами расчета вне контуров

	-> save_results_to_database(self, dict_result, df_exceptions)

		Сохраняет результаты расчета в базу данных

		- dict_result: словарь с результатами расчета
		- df_exceptions: DataFrame исключенных из расчета скважин

	-> stop(self)

		Функция остановки процесса расчета

--- log_window.py

	Содержит класс для вывода окна логирования пользователя и взаимодействия с ним

	-> stop_calculation(self)
		
		Функция остановки при нажатии кнопки прерывания расчета. Главное окно становится активным (при расчете оно неактивно)

	-> on_calculation_finished(self)

		Сигнал о завершении расчета. Главное окно становится активным (при расчете оно неактивно)

	-> closeEvent(self, event)

		Функция, срабатывающая на закрытие окна логирования. Если при закрытии окна идет расчет,
		то он прерывается. Главное окно становится активным (при расчете оно неактивно)

--- main_window.py

	***Класс взаимодействий между элементами главного окна MainWindow

	-> set_default_params(self)

		Загрузка параметров из базы данных по умолчанию

		Возвращает заполненный виджет со значениями параметров расчета

	-> menu_(self)

		Действия на кнопки меню

	-> load_database(self)

		Действие на кнопку загрузки предыдущих рассчитанных результатов

	-> validate_path_db(self)

		Функция валидации пути для сохранения результатов в базу данных.
		Если пользователь вводит несуществующую директорию, путь формируется автоматически

	-> validate(self, dict_params, dict_previous)

		Функция валидации параметров расчета

		- dict_params: текущий словарь параметров, введенных пользователем
		- dict_previous: словарь со значениями параметров до изменения

		Если введенный пользователем параметр имеет некорректное значение,
         	то возвращаются параметры из старого словаря

	-> filedir_widget(self)

		Функция добавления поля ввода пути к файлу данными для расчета

	-> filedir_actions(self)

		Вызов функции валидации словаря параметров при изменении пути к файлу с данными вручную и
        	открытие диалогового окна для выбора файла с данными, показаны файлы только формата .xlsx

	-> open_filedialog(self, filedir_item)

		Функция открытия диалогового окна для помещения пути файла с данными в виджет параметров расчета.
        	Обновление словаря с параметрами и его валидация при изменении пути к файлу с данными

        	- filedir_item: item, в котором находится виджет с путем к файлу с данными

	-> message_box(self, message)

		Функция вывода сообщения для пользователя в отдельном окне

		- message: текст сообщения для пользователя

        	Возвращает  выводится окно об ошибочном вводе параметра расчета

	-> buttons(self)

		Сигналы на нажатия кнопок в главном окне приложения

	-> update_db_path(self)

		Обновление пути к базе данных для сохранения результатов

	-> main_calc_function(self)

		Запуск окна логов и функции расчета в отдельных потоках

	-> log(self, message):

		Задание формата вывода логов для пользователя

        	- message: сообщение с информацией о ходе расчета для пользователя

	-> on_calculation_stopped(self)

		Остановка расчета, обнуление потока расчета и вывод сообщения в окно логов о прерывании расчета

	-> on_calculation_finished(self)

		Сигнал об успешном завершении расчета, разблокировка главного окна, обнуление потока расчета

	-> write_dict(self)

		

	***Класс DataframeToTable перевода DataFrame в формат таблицы для виджета PyQt - QTableView

	-> table_to_excel(self, button_text)

		Сохранение таблицы/таблиц в Excel файл

	-> write_dict(self)

		Считывание параметров расчета из виджета, запись их в БД

	-> editable_column(self, item, column)

		Делает редактируемым только 1 столбец со значениями параметров

        	- item: строка виджета
        	- column: столбец виджета

        	Возвращает отредактированный виджет с возможностью редактировать столбец со значениями параметров

	-> item_clicked(self)

		При нажатии на ячейку в столбце доступном для редактирования открывается возможность редактировать ее значение

	-> clear_layout(self, layout)

		Удаление текущей картинки, чтобы разместить новую

	-> rounding(df)

		Метод возвращает округленные значения в DataFrame

        	- df: DataFrame сводки по сценариям или результат текущего сценария

	-> update_dict_tables(self)

		Обновление словаря с таблицами результатов

	-> update_display(self)

		Обновляет вывод всех результатов в GUI

	-> update_hor_items(self)

		Обновление item для combobox с объектами расчета

	-> update_result_table_picture(self)

		Обновление картинки по значениям combobox_scenario и combobox_horizon

	-> get_result_table(self, table_name)

		Загрузка и вывод таблиц в виджет. Помещает данные из таблицы в БД в виджет окна приложения

        	- table_name: имя таблицы в БД, откуда необходимо выгрузить данные

	-> get_filtered_table(self, horizon)

		Фильтр для таблицы результатов расчета по определенному объекту

        	- horizon: объект, выбранный пользователем

	-> combobox_scenario(self, value)

		Изменяет значение в словаре параметров по ключу при изменении значения combobox

        	- value: значение виджета combobox после изменения

        	Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> combobox_gdis(self, value)

		Изменяет значение в словаре параметров по ключу при изменении занчения combobox

        	- value: значение виджета combobox после изменения

        	Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> combobox_coverage_traj_hw(self, value)

		Изменяет значение в словаре параметров по ключу при изменении значения combobox

        	- value: значение виджета combobox после изменения

        	Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> combobox_average_flowrate(self, value)

		Изменяет значение в словаре параметров по ключу при изменении значения combobox

        	- value: значение виджета combobox после изменения

		Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> combobox_time_boundaries(self, value)

		Изменяет значение в словаре параметров по ключу при изменении занчения combobox

        	- value: значение виджета combobox после изменения

        	Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> combobox_criteria_y_n(self, value)

		Изменяет значение в словаре параметров по ключу при изменении значения combobox

        	- value: значение виджета combobox после изменения

        	Возвращает обновленное значение словаря параметров по ключу item, где находится текущий combobox

	-> update_dict(self)

		Обновление всех значений словаря параметров, тк один из item-ов был изменен

        	Возвращает обновленный словарь значений параметров

	-> get_dict_qtreewidget(self)

		Считывание всех параметров с виджета для ввода параметров

        	Возвращает словарь с параметрами расчета

	-> add_combobox(self)

		Добавление виджета ComboBox в Item, где есть только ограниченный выбор параметров расчета

        	Возвращает отредактированные combobox в виджете параметров

	-> combobox_actions(self)

		Добавление сигналов на изменение QComboBox параметров расчета

	-> closeEvent(self, event)

		Корректное завершение работы при закрытии главного окна приложения

        	- event: событие из главного окна

	***Класс окна для ввода пути к файлу с данными FileDirectWidget.
	   Элемент библиотеки PyQt для ввода текста вместе с кнопкой для открытия файлового диалога windows

	***Класс DataframeToTable для перевода dataframe в модель для размещения в виде таблицы на PyQt.QWidgets.QTableWidget

--- mpl_widget.py

	Скрипт для размещения картинки визуализации данных из полученных таблиц результатов

	***Класс MplWidget для помещения картинки на вкладку "Визуализация". Определяется
	   как фигура matplotlib совместно с панелью для взаимодействия с картинкой:
	   приближение, перемещение, отдаление, сохранение

	-> resizeEvent(self, event)

		Функция отклика на изменение размера окна либо перемещения окна приложения между экранами с разным разрешением

        	- event: событие с виджетом, где размещена картинка

	***Класс CustomNavigationToolbar. Отвечает за панель инструментов работы с картинкой. В классе реализовано
	   индивидуальное сохранение картинки в формате .jpeg размером 25000:25000 пикселей

	-> save_figure(self)

		Функция кастомизации панели инструментов работы с картинкой

--- plot_design.py

	Скрипт для построения картинки по таблицами результатов расчета

	-> plot_results(df_result, script)

		Визуализация полученных результатов в зависимости от выбранного сценария расчета

    		- df_result: DataFrame с результатами расчета по текущему сценарию
    		- script: сценарий расчета

    		Возвращает фигуру matplotlib, координатные оси и чекбокс(скрыть/показать) элементы графика

	-> get_color_area(fond)

		Определение цвета зоны, охваченной исследованием

    		- fond: Фонд, к которому относится скважина

    		Возвращает строку с названием цвета, в который нужно покрасить зону охвата скважины ОС

	-> get_linetype_color(fond, year)

		Выбор стиля и цвета линии обводки в зависимости от фонда и года исследования скважины

    		- fond: фонд скважины
    		- year: год исследования
    		Вовзращает тип и цвет линии в зависимости от года исследования и фонда скважины

	-> get_geometry_color(status)

		Получение цвета геометрии(токи или линии), маркера и подписи скважины на картинке статуса по опорной сети

    		- status: статус скважины по опорной сети

    		Возвращает цвет геометрии, маркер и цвет шрифта для подписи скважины

--- qtsample.py

	Содержит шаблон главного окна приложения: основные виджеты, элементы меню и кнопки.
	Функционал прописывается в скрипте main_window.py в классе MainWindow

--- validate_widget_data.py

	Скрипт содержит 2 класса валидации параметров: для пути к БД и для параметров расчета

	***Класс ValidateData проверяет корректность введенных параметров для расчета

	-> filename(cls, data_file: str) -> str

		Валидация пути к файлу с данными и типа файла

        	- data_file: путь к файлу с данными в строковом виде

		Возвращает путь к файлу, либо вызывает ошибку с выводом сообщения о неверно введенном пути

	-> scenario(cls, calculation_scenario: str) -> str

		Валидация выбранного сценария расчета

        	- calculation_scenario: строка с названием сценария из QComboBox

        	Возвращает сценария расчета

	-> gdis_date(cls, gdis_option: str) -> str | None

		Валидация даты последнего актуального исследования ГДИС

        	- gdis_option: дата последнего актуального исследования ГДИС в виде строки из виджета

        	Возвращает дату в корректном формате или None, если значение пустое

	-> check_boolean_options(cls, value: str) -> bool

		Валидация параметров, имеющих значения да/нет, то есть boolean

        	- value: строковое значение параметра из QComboBox

        	Возвращает логическое выражение: True или False

	-> check_fond_order(cls, list_order_fond: str) -> str

		Валидация порядка фондов для построения регулярной сетки

        	- list_order_fond: строковое значение порядка фондов

        	Возвращает порядок фондов для построения равномерной сетки,
		либо ошибку с сообщением о неверно введенном параметре

	-> string_to_percent(cls, percent_val: str) -> float

		Валидация параметров, определяющихся в процентах. Перевод в число с плавающей точкой

        	- percent_val: строковое значение параметров из виджета

        	Возвращает процент или вызов ошибки с сообщением о неверно введенном параметре

	-> horizon_count_to_int(cls, horizon_count: str) -> int | None

		Валидация параметра кол-ва объектов работы на скважину

        	- horizon_count: строковое значение параметра кол-ва объектов работы на скважину

        	Возвращает значение параметра, либо пустое значение, если параметр не введен, либо вызов ошибки с сообщением
         	о неверно введенном параметре

	-> str_to_empty_or_float(cls, str_to_float_empty: str)

		Валидация параметров, ограничивающих попадание скважин на расчет модуля: дебит жидкости, мин./макс. время исслед.

        	- str_to_float_empty: строковое значение параметра

        	Возвращает значение введенного параметра если он положительный или не введен, либо вывод сообщения об ошибке

	-> watercut_to_float(cls, water_cut: str) -> float

		Валидация значения параметра обводненности

        	- water_cut: строковое значение параметра из виджета

        	Возвращает значение введенного параметра(если => 0 и <= 100) или не введен, либо вывод сообщения об ошибке

	-> correct_mult_coef(cls, mult_coef: str) -> list[float]

		Валидация списка коэффициентов на средний радиус исследования

        	- mult_coef: строковое значение списка коэффициентов из виджета

        	Возвращает значение введенного параметра, если это список из вещественных чисел, либо сообщение об ошибке

	-> limit_coef(cls, float_params: str) -> float

		Валидация параметров, имеющих значение в виде числа с плавающей точкой

        	- float_params: строковые значения параметров с плавающей запятой из виджета

        	Возвращает значение введенного параметра если он положительный или не введен, либо вывод сообщения об ошибке

	-> check_separation(cls, separation_by_years: str) -> int

		Валидация параметра распределения исследования по года в слепых зонах

        	- separation_by_years: значение combobox в формате строки

        	Возвращает целое число

	***Класс ValidatePath для валидации пути к БД для сохранения результатов расчета

	-> def validate_path(cls, path: str) -> str

		Валидация пути к БД

        	- path: путь к БД, введенный пользователем

        	Возвращает путь к БД, либо сообщение об ошибке

--- dictionaries.py

	Скрипт содержит словари для перевода оригинальных названий столбцов таблиц в стандартизированные имена
	для удобства работы с данными, а также словарь для фильтрации статусов скважин.

--- preparing_data.py

	Скрипт подготовки данных. Содержит главную функцию загрузки и подготовки данных. Внутри нее в зависимости от сценария
	выбирается название вспомогательной функции подготовки для определенного типа выгрузки. Также скрипт содержит функцию
	загрузки исключаемых скважин и функцию подготовки .json файла с PVT-свойствами месторождений.

	-> upload_input_data(dict_constant, dict_parameters, log_user, progress_bar):

    		Считывание файла с исключенными скважинами, затем загрузка данных,
    		их подготовка к расчету в зависимости от базы данных
   		и удаление исключенных скважин

    		- dict_constant: словарь со статусами работы скважин
		- dict_parameters: словарь с параметрами расчета
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи
		- log_user: сигнал для передачи сообщения пользователю в окно логирования

   		Возвращается подготовленный DataFrame после считывания исходного файла со скважинами

	-> preprocessing_GeoBD(df_input, dict_constant, dict_geobd_columns, progress_bar)

    		Подготовка данных ГеоБД

 		- df_input: Выгрузка данных ГеоБД
		- dict_constant: статусы и характеры работы скважин
		- dict_geobd_columns: список имен столбцов
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

   		Возвращает DataFrame с необходимыми столбцами для расчета, столбцы в правильном порядке,
    		скважины разделены на ННС и ГС

	-> add_t3_coord_geobd(df, list_columns)

		Подготовка DataFrame к расчету, удаление дубликатов с "_Т3" и добавление координат Т3 первого ствола для ГС

		- df: DataFrame для подготовки к расчету, удаление дубликатов в столбце имен и добавление столбцов с T3
		- list_columns: список столбцов, которые необходимо оставить для дальнейшего расчета

		Возвращает DataFrame скважин с подготовленными координатами

	-> preparing_project_wells(dict_parameters, log_user, progress_bar)

		Чтение файла с проектными скважинами, обработка координат и разделение на типы ННС/ГС

   		- dict_parameters: словарь с параметрами расчета
   		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи
   		- log_user: сигнал для передачи сообщения пользователю в окно логирования

		Возвращает подготовленный DataFrame с проектными скважинами

	-> preparing(dict_constant, df_input, df_exceptions, dict_parameters, progress_bar)

		Подготовка к расчету DataFrame, прошедшего предварительную подготовку в зависимости от типа выгрузки
    
   		- dict_constant: словарь со статусами работы скважин
   		- df_input: DataFrame, полученный из входного файла
   		- df_exceptions: DataFrame исключенных скважин в ходе подготовки к расчету
   		- dict_parameters: словарь с параметрами расчета
   		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

		Возврат DataFrame, подготовленного к расчету

	-> preprocessing_NGT(df_input, min_length_horWell, progress_bar):

  		Подготовка данных из NGT

   		- min_length_horWell: минимальная длина ГС, для разделения скважин на ННС и ГС
   		- df_input: Выгрузка данных NGT
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

    		Возвращается подготовленный DataFrame выгрузки NGT, скважины разделены на ННС и ГС

	-> add_t3_coord_ngt(df, min_length_horWell)

		Добавление координат T3 для всех скважин для возможности создания геометрии

   		- df: DataFrame с данными по скважинам из NGT
   		- min_length_horWell: минимальная заданная длина ГС

		Возвращает DataFrame с подготовленными координатами T1 и T3

	-> upload_gdis_data(df_input, date, dict_parameters):

    		Загрузка данных по проведенным ГДИС на месторождении и удаление из входных данных
    		скважин, на которых проводились исследования начиная с введенной пользователем даты по сей день

		- df_input: DataFrame, полученный путем считывания исходного файла со скважинами
		- date: дата выгрузки файла со скважинами
		- dict_parameters: словарь с параметрами расчета
		- log_user: сигнал для передачи сообщения пользователю в окно логирования
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

		Возвращается DataFrame очищенный от скважин, на которых проводились ГДИС не более n лет назад

	-> geobd_gdis_data(df_input, df_exceptions, dict_parameters, log_user, progress_bar)

		Функция обработки данных ГДИС из выгрузки ГеоБД

		- df_exceptions: DataFrame исключенных скважин в ходе подготовки к расчету
		- df_input: DataFrame, полученный путем считывания исходного файла со скважинами
		- dict_parameters: словарь с параметрами расчета
		- log_user: сигнал для передачи сообщения пользователю в окно логирования
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

		Возвращает DataFrame очищенный от скважин, на которых проводились ГДИС не ранее указанной даты

	-> ngt_gdis_data(df_input, df_exceptions, dict_parameters, log_user, progress_bar)

		Загрузка данных по проведенным ГДИС на месторождении и удаление из входных данных
    		скважин, на которых проводились исследования начиная с введенной пользователем даты по сей день

		- df_exceptions: DataFrame исключенных скважин в ходе подготовки к расчету
		- df_input: DataFrame, полученный путем считывания исходного файла со скважинами
		- dict_parameters: словарь с параметрами расчета
		- log_user: сигнал для передачи сообщения пользователю в окно логирования
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

		Возвращает DataFrame очищенный от скважин, на которых проводились ГДИС не более n лет назад

	-> gdis_preparing(df_gdis, input_wells, year)

		Функция очищает загруженные данные ГДИС от скважин, на которых
		ГДИС проводились раньше указанной пользователем даты

		- df_gdis: данные ГДИС из файла с исследованиями по скважинам, считанные в DataFrame
		- input_wells: имена всех скважин, входящих в исходный файл со скважинами
		- year: опция расчета, задается в формате ДД/ММ/ГГГГ

		Возвращает DataFrame со скважинами, на которых ГДИС проводились более n(year) лет назад

	-> drop_wells_by_gdis(input_row, gdis_objects)

		Функция удаляет объекты для каждой скважины, если по ним проводились ГДИС

		- input_row: текущая строка из входного DataFrame
		- gdis_objects: Series из объектов на которых проводились ГДИС, индексами являются скважины

		Возвращает измененную строку DataFrame или ту же строку, если скважины не оказалось в gdis_object Series

	-> preparing_reservoir_properties(dict_parameters, path, log_user, progress_bar)

		Подготовка PVT свойств из справочника PVT и далее запись в .json файл
		для быстрого дотсупа к свойствам объектов в процессе расчета

		- dict_parameters: словарь с параметрами расчета
		- path: путь к корневой папке
		- log_user: сигнал для передачи сообщения пользователю в окно логирования
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

		Сохраняет словарь в корневую папку в виде json файла со свойствами месторождений

	-> get_exception_wells(dict_parameters, sheet, log_user)

		Загрузка скважин для исключения из расчета или скважин обязательных для включения в ОС в зависимости от имени листа
    		в Excel

		- sheet: имя листа в исходном файле Excel
		- dict_parameters: словарь с параметрами расчета
		- log_user: сигнал для передачи сообщения пользователю в окно логирования

		Возвращает список скважин для исключения

	-> fonds_for_calc(df_horizon, script, percent, cover_criteria, mean_oilrate_option, percent_oilrate)

		Подготовка пьезометров, нагнетательных и добывающих скважин для расчета с учетом списка приоритетных к исследованию
    
		- df_horizon: DataFrame скважин выделенных на текущий объект расчета
		- script: сценарий расчета
		- percent: процент расстояния между точками T1 и T3 необходимый для включения скважины в зону исследования
		- cover_criteria: параметр учета процента расстояния между точками T1 и T3 скважины
		- mean_oilrate_option: параметр учета среднего дебита нефти по объекту
		- percent_oilrate: процент от среднего дебита нефти по объекту

		Возвращает подготовленные DataFrame 3-х фондов, DataFrame приоритетных скважин, DataFrame скважин, охваченных
		приоритетными и средний дебит по объекту расчета

--- save_database.py

	-> results_to_db(dict_result, df_exceptions, dict_parameters, list_name_params, path_database, progress_bar)

		Запись результатов в базу данных

		- dict_result: dict - словарь, содержащий DataFrame результатов для каждого контура
		- df_exceptions: DataFrame - исключенные из расчета скважины
		- dict_parameters: dict - параметры расчета
		- list_name_params: list - список имен параметров расчета
		- path_database: str - путь к файлу базы данных для сохранения результатов
		- progress_bar: сигнал передачи значения в линию прогресса текущей задачи

	-> get_column_mappings()

		Возвращает словарь для переименования колонок из стандартизированных имен в удобочитаемые имена

	-> prepare_database(path_database)

		Создает и возвращает подключение к базе данных, удаляя старую версию базы, если она существует

		- path_database: str - путь к файлу базы данных

		Возвращает sqlite3.Connection - подключение к базе данных

	-> write_contour_data(contour_name, data, df_exceptions, dict_parameters, db_result, dict_rename)

		Записывает данные по контуру в базу данных.

		- contour_name: str - имя контура, используемое как имя таблицы
		- data: tuple - данные контура в виде кортежа (DataFrame, объект контура)
		- df_exceptions: DataFrame - данные исключенных скважин
		- dict_parameters: dict - параметры расчета
		- db_result: sqlite3.Connection - подключение к базе данных
		- dict_rename: dict - словарь для переименования колонок

	-> format_name(name)

		Форматирует имя контура для использования в качестве имени таблицы, сокращая его до 31 символа при необходимости.

		- name: str - оригинальное имя контура

		Возвращает str - отформатированное имя таблицы

	-> filter_exceptions(df_exceptions, contour, dict_parameters)

		Фильтрует исключенные скважины для текущего контура, если контур указан.

		- df_exceptions: DataFrame - данные исключенных скважин
		- contour: объект контура (Polygon или None) - контур для фильтрации скважин
		- dict_parameters: dict - параметры расчета для фильтрации

		Возвращает DataFrame - отфильтрованные данные исключений

	-> prepare_dataframe_for_db(df, df_excluded, contour, dict_rename)

		Подготавливает DataFrame перед записью в базу данных.

		- df: DataFrame - исходные данные контура
		- df_excluded: DataFrame - исключенные скважины для текущего контура
		- contour: объект контура (Polygon или None) - контур для добавления геометрии
		- dict_rename: dict - словарь для переименования колонок

		Возвращает DataFrame - подготовленные данные для записи

	-> save_report_to_db(db_result, dict_result, dict_parameters, list_name_params)

		Сохраняет отчетные данные и параметры расчета в базу данных.

		- db_result: sqlite3.Connection - подключение к базе данных
		- dict_result: dict - результаты расчетов
		- dict_parameters: dict - параметры расчета
		- list_name_params: list - список имен параметров

	-> get_report(dict_result)

		Функция для создания краткого отчета по всем контурам с разными коэффициентами для радиусов охвата

		- dict_result: словарь с результатами расчетов по всем объектам

		Возвращает DataFrame с отчетом по каждому контуру с определенным коэффициентом увеличения радиуса


-----------------------------------------------The End-----------------------------------------------